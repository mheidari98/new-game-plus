---
// /deals/ was the explorer's first name, and is linked from the README.
import Redirect from '../layouts/Redirect.astro';
const base = import.meta.env.BASE_URL;
---
<Redirect to={base} note="This page is now the front page." />
