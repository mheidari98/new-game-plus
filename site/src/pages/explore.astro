---
// /explore/ was the explorer's own route before it became the front page.
import Redirect from '../layouts/Redirect.astro';
const base = import.meta.env.BASE_URL;
---
<Redirect to={base} note="Explore is now the front page." />
