# Pin the public registry. Without this, a contributor whose global npm points
# at a private mirror bakes that hostname into package-lock.json, which then
# breaks CI (E401) and leaks internal infrastructure into a public repo.
registry=https://registry.npmjs.org/
